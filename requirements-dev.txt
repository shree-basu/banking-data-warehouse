# Third-party dependencies for local and pull-request validation.
# Install the repository itself separately with: python -m pip install --no-deps -e .
Faker==40.37.0
pytest==8.3.5
ruff==0.11.2
sqlfluff==3.3.1
